apiVersion: v1
kind: Service
metadata:
    name: revaturecinephiliacsforumrepo
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: revaturecinephiliacsforumrepo